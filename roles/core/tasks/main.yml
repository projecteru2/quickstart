---
- name: Configure core
  ansible.builtin.import_tasks: core_config.yml
  tags:
    - core_config

- name: Launch core
  ansible.builtin.import_tasks: core_launch.yml
  tags:
    - core_launch

- name: Install eru-cli
  ansible.builtin.import_tasks: core_cli.yml
  tags:
    - core_cli

- name: Create the pod
  ansible.builtin.import_tasks: core_resource.yml
  tags:
    - core_resource
