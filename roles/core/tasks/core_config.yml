---
- name: Ensure core config directory
  ansible.builtin.file:
    path: "{{ core_config_dir }}"
    state: directory
    mode: '0755'

- name: Render core config
  ansible.builtin.template:
    src: core.yaml.j2
    dest: "{{ core_config_dir }}/core.yaml"
    mode: '0644'
